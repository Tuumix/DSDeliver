export interface ProductProps{
    id: number;
    name: string;
    price: number;
    description: string;
    imageUri: string;
}

export interface OrderLocationdata{
    latitude: number;
    longitude: number;
    addres: string;
}